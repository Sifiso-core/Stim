services:
  # stimapi:
  #   image: stimapi
  #   build:
  #     context: .
  #     dockerfile: src/Stim.Api/Dockerfile
  #   ports:
  #     - 5000:5000
  #     - 5001:5001
  stim.postgresDb:
    image: postgres:17.2 # or latest
    environment:
      POSTGRES_DB: stimDb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - ./.containers/postgres_data:/var/lib/postgresql/data
    ports:
      - 5432:5432
